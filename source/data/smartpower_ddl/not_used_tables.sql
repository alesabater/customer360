
CREATE table_discount(
		BUSINESS_PARTNER_NO INT NOT NULL,
		CONTRACT_NO INT NOT NULL,
		DISCOUNT_PACKAGE_ID INT NOT NULL UNIQUE,
		DISCOUNT_FROM_DATE DATE NOT NULL,
		DISCOUNT_TO_DATE DATE NOT NULL,
		DISCOUNT_PACKAGE_DESC VARCHAR(50) NOT NULL,
		DISCOUNT_PACKAGE_PCT DOUBLE NOT NULL,
	PRIMARY KEY(DISCOUNT_PACKAGE_ID)
)

USE marketing

CREATE TABLE json_marketing_preference(
		BUSINESS_PARTNER_NO VARCHAR NOT NULL UNIQUE,
		COMMUNICATION_TYPE VARCHAR NOT NULL,
		CONSENT_CODE INT NOT NULL,
		CONSENT_DESCRIPTION VARCHAR NOT NULL,
		CONSENT_FORMAT VARCHAR NOT NULL,
		VALID_FROM DATE NOT NULL,
		VALID_UNTIL DATE NOT NULL,
	PRIMARY KEY(BUSINESS_PARTNER_NO)
)

USE call_centre

CREATE TABLE csv_acd_calls(
		DEV_ID VARCHAR NOT NULL,
		DEV_TIMESTAMP LONG NOT NULL,
		TOTAL_CONSUMPTION DOUBLE NOT NULL,
		DATE DATE NOT NULL,
		TIME TIME NOT NULL,
		CONSUMPTION DOUBLE NOT NULL,
		DAY_POINT INT NOT NULL,
		MINUTE_POINT INT NOT NULL,
		MPRN VARCHAR NOT NULL UNIQUE,
	PRIMARY KEY(MPRN)
)

CREATE TABLE csv_ivr_calls(
		DEV_ID VARCHAR NOT NULL,
		DEV_TIMESTAMP LONG NOT NULL,
		TOTAL_CONSUMPTION DOUBLE NOT NULL,
		DATE DATE NOT NULL,
		TIME TIME NOT NULL,
		CONSUMPTION DOUBLE NOT NULL,
		DAY_POINT INT NOT NULL,
		MINUTE_POINT INT NOT NULL,
		MPRN CHAR(14 CHAR) NOT NULL UNIQUE,
	PRIMARY KEY(MPRN)
)